import React from "react"
import Layout from "../components/layout"

const IndexPage = () => (
  <Layout>
    <div style={{
      fontFamily: 'Roboto, sans-serif',
      color: `black`,
      fontSize: `14px`,
      fontWeight: `normal`,
      padding: `20px 0`
    }}>
    Simple Application using gatsby
  </div>
  </Layout>
)

export default IndexPage
